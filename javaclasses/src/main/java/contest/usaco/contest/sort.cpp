/*
ID: agentmz1
PROG: test
LANG: C++
*/

#include <iostream>
#include <fstream>
#include <vector>
#include <list>
#include <algorithm>

using namespace std;
#define LP(name, minVal, maxVal) for(int name = minVal; name < maxVal; name++)

/* var dec */

/* func dec */

int main() {
    ifstream stdin ("sort.in");
    ofstream stdout ("sort.out");
    /* init */
    int n;
    stdin >> n;
    long long s[n];
    /* run */
    /* exit */
    cout << endl;
    stdin.close();
    stdout.close();
    return 0;
}
